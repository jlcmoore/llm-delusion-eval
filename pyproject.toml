[project]
name = "llm-delusion-eval"
version = "0.1.0"
description = "Evaluation tooling for delusion-related behavior in language models."
readme = "README.md"
authors = [
    { name = "Jared Moore", email = "jlcmoore@stanford.edu" },
    { name = "Andrea Mock", email = "amock@stanford.edu" },    
    { name = "Yifan Mai", email = "yifan@cs.stanford.edu" }
]
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.75.0",
    "google-genai>=1.43.0",
    "huggingface-hub>=0.35.0",
    "inspect-ai>=0.3.136",
    "matplotlib>=3.9.0",
    "llm-delusions-annotations",
    "numpy>=2.0.0",
    "pandas>=2.2.0",
    "pyarrow>=17.0.0",
    "plotly",
    "kaleido",
    "openai>=2.11.0",
    "seaborn>=0.13.0",
    "torch>=2.3.0",
    "transformers>=4.44.0",
    "xai-sdk>=1.11.0",
]

[project.scripts]
llm-delusion-eval-generate-report = "llm_delusion_eval.scripts.generate_report:main"
llm-delusion-eval-serve-report = "llm_delusion_eval.scripts.serve_report:main"
llm-delusion-eval-export-spiral-web-report = "llm_delusion_eval.scripts.export_spiral_web_report:main"

[project.optional-dependencies]
google = [
    "google-genai>=1.43.0",
]
openai = [
    "openai>=2.2.0",
]
anthropic = [
    "anthropic>=0.72.0",
]
context = [
    "llm-delusions",
]
# together = [
#   "together>=1.5.31",
# ]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pre-commit>=4.3.0",
    "pylint==3.3.1",
    "ruff==0.14.0",
    "vulture>=2.15",
]

[tool.uv.sources]
llm-delusions-annotations = { git = "https://github.com/jlcmoore/llm-delusions-annotations.git" }
llm-delusions = { git = "ssh://git@github.com/jlcmoore/llm-delusions.git" }

[tool.ruff]
# E: pycodestyle errors, F: Pyflakes, W: pycodestyle warnings, I: isort, PL: Pylint
lint.select = ["E", "F", "W", "I", "PL"]
